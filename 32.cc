#include <bits/stdc++.h>
using namespace std;

// use something like next permutiation
// have to mutliply single digit numbers by 4 digit 
// and 2 digit numbers wiht 2 digit
// its only till 100 since bigger numbers will be convered when
// we rearrange the rest of the numbers to form pandigital 
int main() {
    int end = 99;
    for(int i = 1; i<10 ;i++) {
    }
    return 0;
}
